[defaults]
# Use our mock modules first, fall back to built-ins
library = ./library

# Don't require host key checking for localhost
host_key_checking = False

# Better output formatting
stdout_callback = yaml

# Reduce verbosity of skipped tasks
display_skipped_hosts = False

# Don't gather facts (faster tests)
gathering = explicit
