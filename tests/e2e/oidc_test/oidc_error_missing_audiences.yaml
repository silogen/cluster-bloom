# tests/e2e/oidc_test/oidc_error_missing_audiences.yaml
# E2E Test case: Missing audiences should cause validation error
ENABLED_STEPS: PrepareRKE2Step
DOMAIN: test.local
FIRST_NODE: true
GPU_NODE: false
USE_CERT_MANAGER: false
CERT_OPTION: generate

ADDITIONAL_OIDC_PROVIDERS:
  - url: "https://kc.test.local/realms/k8s"
    # Missing audiences field - should cause error

# Note: E2E tests don't use expected_error field like integration tests
# This test will show the actual error output in the VM