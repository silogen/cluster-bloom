# tests/integration/step/10_PrepareRKE2Step/09-tls-san-basic-auto-only/bloom.yaml
# Test case: Basic TLS-SAN auto-generation (k8s.<domain> only)
ENABLED_STEPS: PrepareRKE2Step
DOMAIN: test.local
FIRST_NODE: true
GPU_NODE: false
USE_CERT_MANAGER: false
CERT_OPTION: generate

# No ADDITIONAL_TLS_SAN_URLS - testing auto-generation only
# Expected: Only k8s.test.local should be generated

mocks:
  PrepareRKE2.RunCommand.modprobe:
    output: ""
    args: ["iscsi_tcp"]
  PrepareRKE2.RunCommand.modprobe:
    output: ""
    args: ["dm_mod"]
  PrepareRKE2.RunCommand.mkdir:
    output: ""
    args: ["-p", "/etc/rancher/rke2"]
  PrepareRKE2.RunCommand.chmod:
    output: ""
    args: ["0755", "/etc/rancher/rke2"]