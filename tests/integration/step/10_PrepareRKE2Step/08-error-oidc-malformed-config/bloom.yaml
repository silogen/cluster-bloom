# tests/integration/step/10_PrepareRKE2Step/08-oidc-malformed-config/bloom.yaml
# Test case: Malformed OIDC configuration structure
ENABLED_STEPS: PrepareRKE2Step
DOMAIN: test.local
FIRST_NODE: true
GPU_NODE: false
USE_CERT_MANAGER: false
CERT_OPTION: generate

ADDITIONAL_OIDC_PROVIDERS:
  - url: "https://kc.test.local/realms/k8s"
    # Missing audiences field entirely

expected_error: "missing required 'audiences' field"

mocks:
  PrepareRKE2.RunCommand.modprobe:
    output: ""
    args: ["iscsi_tcp"]
  PrepareRKE2.RunCommand.modprobe:
    output: ""
    args: ["dm_mod"]
  PrepareRKE2.RunCommand.mkdir:
    output: ""
    args: ["-p", "/etc/rancher/rke2"]
  PrepareRKE2.RunCommand.chmod:
    output: ""
    args: ["0755", "/etc/rancher/rke2"]