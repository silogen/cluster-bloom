# tests/integration/step/10_PrepareRKE2Step/13-error-tls-san-invalid-additional/bloom.yaml
# Test case: Invalid domains in additional TLS-SANs
ENABLED_STEPS: PrepareRKE2Step
DOMAIN: test.local
FIRST_NODE: true
GPU_NODE: false
USE_CERT_MANAGER: false
CERT_OPTION: generate

ADDITIONAL_TLS_SAN_URLS:
  - "invalid..domain"     # Invalid: double dots
  - "api.test.local"      # Valid: mixed with invalid

# Expected: Should succeed - no domain validation in backend
# Tests that invalid domains pass through to certificate generation (intended behavior)

mocks:
  PrepareRKE2.RunCommand.modprobe:
    output: ""
    args: ["iscsi_tcp"]
  PrepareRKE2.RunCommand.modprobe:
    output: ""
    args: ["dm_mod"]
  PrepareRKE2.RunCommand.mkdir:
    output: ""
    args: ["-p", "/etc/rancher/rke2"]
  PrepareRKE2.RunCommand.chmod:
    output: ""
    args: ["0755", "/etc/rancher/rke2"]