name: Integration Tests

on:
  workflow_dispatch:

jobs:
  integration-tests:
    runs-on: pd4
    steps:
      - uses: actions/checkout@v4

      - name: Build with devbox
        run: devbox run build

      - name: Run unit tests
        run: devbox run go test -v ./pkg/...

      - name: Run integration tests
        run: devbox run go test -v ./tests/integration

      - name: Run UI tests
        run: |
          # Start Chrome if not already running
          if ! docker ps --format '{{.Names}}' | grep -q '^chrome-test$'; then
            docker run -d --rm \
              --name chrome-test \
              --net=host \
              -e "PORT=9222" \
              browserless/chrome:latest

            # Wait for Chrome to be ready
            sleep 5
          else
            echo "Chrome container already running"
          fi

          # Run UI tests
          devbox run go test -v ./tests/ui

      - name: QEMU Disk Test
        run: |
          bash tests/e2e/qemu/qemu-disk-test.sh nvme-test-vm tests/e2e/nvme_8_disks/profile.yaml dist/bloom

      - name: Check Test Results
        run: |
          SUCCESS=$(yq '.overall_summary.success' nvme-test-vm-test-results.yaml)
          if [ "$SUCCESS" != "true" ]; then
            echo "❌ Tests failed - check nvme-test-vm-test-results.yaml for details"
            cat nvme-test-vm-test-results.yaml
            exit 1
          fi
          echo "✅ All tests passed"
