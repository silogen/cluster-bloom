# Test configuration for dry-run mode
# This config will only run ValidateSystemRequirementsStep

# Enable only the validation step
ENABLED_STEPS: "ValidateSystemRequirementsStep"

# Basic required configuration
FIRST_NODE: true
GPU_NODE: false
CONTROL_PLANE: true
NO_DISKS_FOR_CLUSTER: true

# Domain configuration (required but not used in this step)
DOMAIN: "test.local"
USE_CERT_MANAGER: false

# Cluster configuration
CLUSTER_NAME: "test-cluster"
