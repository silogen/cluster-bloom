# The bundled Ansible runtime rootfs is generated by `just fetch-image` from the
# pinned ImageRef (see pkg/ansible/runtime/container.go). It is ~120MB and is
# not committed; regenerate it before an offline build (`just build-offline`).
/ansible-rootfs.tar.gz
